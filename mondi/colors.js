var bgColor;
var team1color, team2color, bloodColor;
var textColor, groundColor, skyColor;

function colorDeclaration() {
    bgColor = color(120);

    // endloss
    team1color = color(50);
    team2color = color(220);
    bloodColor = color(100,0,0);

    // I mouth
    textColor = color(50);
    groundColor = color(50);
    skyColor = color(220);
}